# Examples

`ghost-ai-context/context/` is a real example of the kind of folder ARCH is meant to produce.

It is intentionally project-specific. Do not copy it as a default template; use the templates bundled in `arch/assets/context-template/` and let ARCH fill them for the actual project.
